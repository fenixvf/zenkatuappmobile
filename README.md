# Zenkatu  — PWA
Generated by Site2App.
## How to install
1. Extract this ZIP to a folder.
2. Host the folder on any static web server (Vercel, Netlify, GitHub Pages, etc.).
3. Open the hosted URL in a mobile browser (Chrome on Android, Safari on iOS).
4. Use the browser's 'Add to Home Screen' / 'Install App' option.
## Files
- `manifest.json` — Web App Manifest with app metadata
- `sw.js` — Service Worker for offline caching
- `index.html` — Entry page that loads https://zenkatu.base44.app/
- `icon.png` — App icon